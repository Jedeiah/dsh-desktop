# Changelog

## 0.5.7（未发布）

### 新增

- **氛围背景跟随鼠标（设计稿方案 7：视差分层 + 邻近点亮）**：工作台让位、氛围层露出时（开抽屉 / ⌘K 命令面板）**以及启动加载页**，鼠标在背景上移动会触发两种反馈——① **视差分层**：光晕/网格/图标/信息块按深度反向位移（≤26px，缓动尾随，移出即回位）；② **邻近点亮**：鼠标附近的网格成片提亮、信息块文字被"点亮"（发光），快速划动留下涟漪，靠近图标时图标被"照亮"。
  三项硬约束（用户明确要求「排版不要动」）：
  - **排版零改动**：只新增装饰层（`.ambient-pulse` 点亮网格 / `.ambient-ripple` 涟漪 / `.ambient-lit` 点亮副本），全部绝对定位、`pointer-events:none`、脱离文档流；原有 `.ambient*` 规则一条未改。
  - **启动加载页同样生效（页面内容不变）**：图标做视差 + 靠近发亮，标题用「纯文本副本 + 移动遮罩」点亮（原文一个字不动、不加任何元素到流内），环境层在衬底后照旧轻动。
  - **点亮文字不拆原文**：用「信息块整块副本 + 跟随鼠标的径向遮罩显影」，而不是把原文拆成逐字符 span——实测拆分会被迫打断文本整段 shaping（字宽 +0.16px、居中位置漂移 0.01px），属改动排版，已弃用并写入注释。
  - **静止态像素级等于改造前**：`will-change`/过渡只在交互期间挂 `.ambient-parallax` 类（用完即摘），因此不给模糊光晕与遮罩网格换栅格化路径；本机用「同页面对照 + PNG 逐像素比对」验证：静止时新旧渲染**字节完全一致**。
  开销与降级：仅在浮层打开（背景真正露出）时挂监听，平时零开销；mousemove 经 rAF 合并、按需写 transform/opacity/CSS 变量（量化的 filter 写入）；字符/基准位置只在浮层开关、抽屉开关、尺寸变化、字体就绪时重算；`prefers-reduced-motion`、页面隐藏、鼠标在浮层上时自动收敛或不启用。

## 0.5.6 — 工作台「模型/推理等级」点选不生效（macOS WebKit）（2026-09-18）

### 修复

- **工作台里点「模型／推理等级」选项不生效（macOS 专有）**：同一份 dsh 在浏览器里点得动、在 App 里点完还是原来的值。根因是**引擎对「鼠标按下」的焦点语义差异**，被 dsh 自己的浮层关闭逻辑放大：
  dsh 的模型/等级选择器（`dsh-client-ui-model-selection`）用两条判据决定浮层要不要关——① `document` 的 `mousedown` 落在 root/菜单之外；② 根节点 `onBlur` 时新焦点不在 root/菜单之内。② 依赖 Blink 的行为「按下按钮会把焦点移到该按钮」：Chrome 实测按下选项 → `focusout.relatedTarget` = 被按选项（在菜单内）→ 不关；而 macOS WebKit（WKWebView/Safari）按下按钮**不移动焦点**，焦点丢给 `body` 且 `relatedTarget` 为 `null` → `relatedTarget instanceof Node` 为假 → 判成「焦点离开浮层」→ 浮层在按下瞬间被卸载，位置被下面的输入框接管 → 抬起时 WebKit 不再派发 `click` → 选择从未提交。
  证据链：同一份 dsh v0.1.6-alpha.2、同一套真实鼠标事件（Chrome 用 CDP 真实事件；macOS 侧用临时 WKWebView 垫片程序合成真实 NSEvent），同一份探针记录 `pointerdown/mousedown/focusout/focusin/pointerup/mouseup/click` + `document.activeElement` + 菜单存在性与位置 + `Node.contains` 调用结果：WebKit 侧 `focusout … rel=null`（Chrome 侧 `rel=BUTTON.option|…`）后 `MENU=null`、无 `click`。
  修法：工作台 webview 注入一段垫片（`workbench.rs` 的 `WEBKIT_MENU_SHIM_JS`，仅 macOS 注入）做两件事：
  ① **拦掉「菜单项失焦到 body/无处」的 focusout**（`stopPropagation`，让 React 根上的 onBlur 收不到）——浮层不关、click 正常派发；判据很窄（旧焦点属 `role=menu/listbox` 且新焦点为 body/空），点浮层外仍由「mousedown 落在浮层外」那条正常关闭，输入框等其它失焦不受影响。**不移动焦点、不 preventDefault**，无焦点副作用。
  ② **对齐 Blink 的焦点环策略**：同一引擎差异还有个副作用——WebKit 里鼠标点击不移动焦点，于是「上次输入是键盘」（用户在输入框打过字）之后，WebKit 会把**脚本聚焦**判成 `:focus-visible`，dsh 自己的 `.focus()`（打开二级菜单时聚焦当前项、选中后把焦点还给输入框上的模型按钮）就会画出焦点环——即用户看到的「选中框」（浏览器里不画）。故记录最后一次输入模态，只在指针模态下抑制 `:focus-visible` 的 outline/box-shadow（input/textarea/select/contenteditable 排除在外，Blink 对它们点击也画环）；键盘操作时环照常出现。
  Windows（WebView2/Blink）本就是 Chrome 语义，注入空串保持调用点一致。**上游修好后可删**（判据改成「按下期间的 blur 不算离开」即无需垫片）。

## 0.5.5 — App 更新提醒 + 插件更新（2026-09-17）

### 新增

- **App 启动后自动检查更新并提醒**：壳页加载/重载时调 `app_update_probe_cmd`（后端保证**每次启动至多一次**网络检查，异步、失败静默），有新版时三处提示同时亮起：
  - **顶栏「管理」按钮上的呼吸灯红点**——顶栏是唯一**始终可见**的区域（工作台盖住壳页时壳页只剩 36pt），不打开抽屉也能看到；`1.6s` 呼吸动画，`prefers-reduced-motion` 下退化为常亮；
  - **关于 tab 上的小点**（用户不一定会切到「关于」分段）；
  - **关于面板顶部横幅**「App 有新版本 vX.Y.Z · 下载并安装」，与 dsh 面板那条横幅同款。
  检查结果**缓存在后端**（不只前端变量）——壳页 reload（重启 dsh、切主题等）后红点与横幅不会丢；手动点「检查更新」也走同一条写缓存 + 广播路径，保证两处结论一致。
  交互边界：**更新进行中三处提示一律隐藏**（进度条已在关于面板显示，避免"发现新版本"与进度同屏）；「下载并安装」抽成唯一入口 `startAppInstall`，关于面板主按钮与横幅按钮共用（两份流程必然走岔）；不自动下载/安装；默认不发系统通知。

- **插件「检查更新」与「更新」按钮**（插件分段 → 已安装插件面板头）：对 **npm 来源**的插件逐个查 registry 的 `dist-tags.latest`，行内显示 `1.0.0 → 2.0.0` 并在该行出现「更新」按钮；点「更新」**直接升到最新**（走 `dsh plugin add <name>@latest`，把 profile 里的依赖范围重写为最新，而不是 pnpm update 那种"只在声明范围内升"）。**跨大版本时二次确认**（提示可能不兼容当前 dsh）。
  两条刻意的取舍：① 用 `node_modules` 里的**实际安装版本**比对（profile 里的 spec 是 `^1.0.0` 这类范围，拿它比会把同范围内的新版误判成已最新）；② **Git / URL / 本地路径来源不参与比对**（Git 可能被 `#ref` 钉住、本地路径没有"最新版"概念，猜出来的"有更新"会骗人）——这些行只显示来源，更新请用上方输入框重填。单个插件查询失败只记在该条上，不影响其它。

### 修复

- **插件输出面板不再无限往下延伸**：`theme.css` 里插件段的 `max-height: none`（本意"吸收剩余高度"）让日志框随行数一直变长——渲染实测 600 行时元素高 **24502px**，整个抽屉被顶下去。现在封顶 320px（内部滚动，`.log` 基类本就 `overflow-y:auto`）。
- **插件输出的三处界**（回答"不清理会不会有内存问题"）：① 行数——前端**早已**只保留最近 500 行（`shell.js` 的 `logLine`）；② 单行长度——新增封顶 2000 字符（有些包 postinstall 一次打出一行几十 KB）；③ 后端收集与 IPC 载荷——新增每流 256KB 上限（保留末尾 + 截断说明，UTF-8 安全），此前会把整段输出无界存进 String 并再传一遍给前端。

## 0.5.4 — macOS 卸载后 cookie 被 WebKit 在退出瞬间写回（2026-09-17）

### 修复

- **macOS 卸载后 `~/Library/HTTPStorages/<id>.binarycookies` 会重新出现**（真机实测）：卸载流程里已经删过它（本进程内删了两轮：常规删除 + 等 900ms 的补删），但 WebKit 会在**本进程退出那一刻**再把 cookie 落盘——实测该文件 mtime 与「App 被移入废纸篓后 `killall Dock`」同一秒，而其它所有目标都清得干干净净，说明删除成功、是删完之后又被写回来。
  修法：卸载的 macOS 收尾额外挂一个**脱离本进程的短命 shell**（`/bin/sh -c "sleep 3; rm -rf …; rm -f …; defaults delete <id>"`），等本进程彻底退出后按同一份清单再删一遍。清单与 `uninstall_targets` 同源（绝对路径 + 末段含 bundle id），并**排除 app_data**——万一用户在这几秒内重装启动，新实例刚写下的数据不能被误删。

## 0.5.3 — Windows App 内更新改为「更新助手」；卸载链修复与残留提示（2026-09-17）

### 修复

- **Windows 版 App 内更新「程序退出后就没反应、也没更新」**。三段已核对的机制（tauri-bundler 2.9.4 的 NSIS 模板 + NSIS 源码）：
  1. `/S` 静默安装器只按**主程序名**杀进程（`CheckIfAppIsRunning` → `KillProcessCurrentUser "${MAINBINARYNAME}.exe"`），而本 App 拉起的 dsh 子进程跑的是安装目录里的 `resources\node\node.exe`，不在其中；Windows 上「正在运行的映像文件不可写」。
  2. **被占用的文件不会让安装失败，而是被静默跳过**：NSIS 默认 `AllowSkipFiles=on` + 静默默认 `IDIGNORE`（`exec.c` / `util.c`）→ 跳过该文件继续装，**退出码仍是 0**、`.onInstSuccess` 照常执行。
  3. `/R` 的 `RunAsUser` 拉起新版**不检查返回值**；旧实例还在时，新进程会被单实例插件转发给旧实例后自行退出。
  合起来即：安装器没杀掉我们 → 主程序 exe 被静默跳过 → 安装器报成功 → 本进程 `app.exit(0)` → `/R` 拉起的新进程被单实例吞掉 → 桌面什么都不剩、版本还是旧的。
  现在改为**外部更新助手**：先结束 dsh / pnpm / 孤儿子进程（释放 `$INSTDIR` 文件锁）→ 助手写握手文件 → **等本进程退出**（并再确认一次真的退出了）→ 才静默安装（`/S /R`）→ 装完若一个实例都没起来则补一次启动。本进程只在等到握手后才 `exit`；助手起不来（如 PowerShell 被策略禁用）则当场报错、结束助手进程并保住 App。助手脚本用真实 PowerShell 按生产调用形态实测：①进程仍在跑时不启动安装器（等到进程消失才装）；②等待窗口结束仍在跑则**放弃安装**（宁可不装，也不在 App 活着时装）；③装完已有实例则不重复拉起；④无实例时补启动
- 安装器没能接管（助手未就绪）时把 dsh 重新拉起且**不刷新壳页**（`respawn_dsh`，避免把刚弹出的失败提示冲掉）
- dsh 安装的进度文案不再出现「一行加号」：pnpm 在 `Packages: +N` 之后会输出一条纯 `+` 的条形进度（实测观测到 60 与 80 两种长度；宽度由 pnpm 按终端列宽决定，与过滤无关），安装页把它当阶段文案逐行显示。现在这类纯符号行在流式回调处即被过滤（真实内容行如 `+ @deepseek-ai/dsh 0.1.5-rc.2` 不受影响）

### 变更

- **注意：更新链路的新逻辑在 0.5.3 及以后版本里生效。** 从 0.5.2 及更早版本**应用内**更新到 0.5.3 时，执行更新动作的仍是旧版本里的旧逻辑（可能仍表现为「退出后没反应」）；建议先到 Releases 手动安装一次 0.5.3，之后的应用内更新才走新链路。若旧逻辑失败，下次启动会按下面的标记提示「上次更新到 vX 未生效」
- App 更新尝试落盘标记（`<app-data>/update-attempt.txt`）：安装器接手前写入，**下次启动**核对——生效记一行日志；未生效则日志 + 系统通知明确提示「上次更新到 vX 未生效」并给出日志路径。标记分两阶段（发起 / 助手已开始安装），据此选等待窗口：安装已开始等 120s，仅发起等 60s，避免用户在装完前又双击旧版造成假警报
- Windows 更新期间不再删除 `%TEMP%` 里的安装包（助手在本进程退出后才用它，失败时也要留着重试），由启动时的 `sweep_stale_installers`（>1h）兜底
- 更新进行中对会拉起 node/dsh 的命令加后端互斥（`dsh_restart_cmd` / `setup_dsh_cmd` / `update_dsh_cmd` / `plugin_op`，以及插件操作与 dsh 安装完成后的内部自动重启）：这些操作会重新占住 `resources\node\node.exe` 的映像锁，让安装器静默跳过该文件
- 修正 `kill_dsh` 的「主动停止」标志：仅在确有子进程被结束时置位（此前重复调用会把下一次真实崩溃误判为主动停止，令其不自愈、不计数）

### 卸载

- **macOS 补清 `~/Library/HTTPStorages/<bundle-id>/`（目录形态）**：此前只删 `<bundle-id>.binarycookies`（文件形态），而现代 macOS 上目录形态（内含 `httpstorages.sqlite`）更常见——本机实测 `~/Library/HTTPStorages` 下 91 个目录形态、12 个 `.binarycookies` 文件形态。沙箱实测：修前目录形态残留、修后被清
- **卸载时清掉临时区的更新包**：`dsh-desktop-update-*.dmg|.exe`（几十 MB）此前只在 App 运行期间的启动清扫里按 1 小时 TTL 处理，卸载后就永远留下了。现在卸载流程按名字强制清（App 内卸载在更新期间被前端锁+后端门挡住；从系统「设置 → 应用」触发的卸载绕过 UI，极端情况下若正好在下载同一文件，会让那次更新以校验失败告终——后果有界、可重试），实测只删本 App 前缀、同目录其它文件不动
- **Windows：安装版但系统卸载器起不来时不再冒充便携版**：此前只要 `uninstall.exe` 没能启动（安全软件拦下/权限）就走便携版分支——删除用户数据、并提示"便携版需手动删除文件夹"，而程序文件其实还在。现在保持可用状态（程序文件与 app 数据都不动；只有「删除 ~/.dsh」档位已按你的选择在拉卸载器**之前**删掉 ~/.dsh，提示里会说明），弹「卸载未完成：无法启动系统卸载器…」并可重试
- **卸载按"实际形态"删，而不是按登记表**：同一位置在不同系统/版本上可能是文件也可能是目录（`HTTPStorages` 的 `<id>.binarycookies` 与 `<id>/` 就是典型）。此前登记为目录却实际是文件（或反之）时会报 `NotADirectory`/`IsADirectory` → 既误报"延迟清理"又真的删不掉。现在自动按 `is_dir()` 选择删除方式
- **Windows 卸载残留现在会明确提示用户（含静默卸载）**：NSIS 的 `Delete`/`RMDir` 失败是**静默**的（源码核实：只置内部错误位，不弹框、不中断），而本 App 内卸载又是 `/S` 静默的、App 已退出没有界面可回显 → 一旦有文件被占用（node 没退干净、杀软扫描中），用户只会看到"卸载成功"、目录却还在。现在卸载收尾（`NSIS_HOOK_POSTUNINSTALL`）核对 `$INSTDIR` / `%APPDATA%\<id>` / `%LOCALAPPDATA%\<id>` 三处，**真有残留才**弹一次提示（MessageBox 不带 /SD，静默模式同样显示），并列出要手动删的目录
- `installer-hooks.nsh` 注释与实现对齐：本 App 并没有开机自启功能，`Run` 键删除是幂等兜底；补上现代 Windows 上真正需要清理的 `%LOCALAPPDATA%\<id>`（WebView2）与 `%APPDATA%\<id>`（app 数据）

## 0.5.2 — App 更新体验：更新中锁定其它操作 + 下载进度（2026-09-16）

### 新增

- **App 更新下载进度**：关于页下载更新时显示进度条与「已下载 / 总量」，校验安装阶段切为不确定态文案（后端按整数百分比变化发 `app:update-progress` 事件）

### 修复

- **App 更新进行中不再允许其它操作**：此前「检查更新」仍可点击，会把「下载并安装」按钮重新启用，连点可触发两次并发更新（下载两份安装包、拉起两次安装器）。现在进入单一「更新中」状态：检查更新 / 下载 / 卸载两档 / dsh 分段的安装·切换·回滚·更新 全部禁用（只读与「在浏览器打开下载页」保留），结束后按"锁前状态"精确恢复；后端另加并发门拒绝第二次调用
- 下载改为分块拷贝以统计进度（保留原有的字节数校验与 SHA-256 校验）


## 0.5.1 — 命令面板「重启 dsh」与交互修复（2026-09-15）

### 新增

- 命令面板新增「**重启 dsh**」：结束并重新拉起 dsh 运行时（用于在终端改过插件/配置后让 App 重新加载；有二次确认，因为会中断正在运行的任务）。从命令面板触发是一次**显式重启**——工作台会显示并重连到新端口；安装/更新进行中会拒绝（避免撞上 pnpm 与半成品 tmp 目录）

### 修复

- dsh 版本列表：首行「最新」徽标此前永不出现——徽标与升级判定都基于 npm `dist-tag`，而它可能落后于最新已发布版本（实测 `dist-tags.latest = 0.1.5-rc.1`，而当时最新已发布为 0.1.6-alpha.1）。改为以**版本列表里的 semver 最大值**为准（dist-tag 仅作兜底），升级判定改用语义比较——顺带消除「提示降级到旧版本」与「更新按钮装旧版」两个连带问题
- 插件装卸 / dsh 版本更新后不再把用户从抽屉里踢出去：`restart_dsh` 在**浮层占用工作区时（抽屉 / 命令面板 / 确认弹窗）**不再移入原生工作台、也不再整页刷新壳页（此前会把插件输出面板与分段状态全部清空）；`reveal_main_window` 只在窗口原本隐藏时（真正的召回）才收起浮层并移入工作台
- 确认弹窗从**命令面板**触发时此前会被原生工作台盖住：`runPaletteItem` 先 `closePalette()` → 工作台被移回窗口、壳页被裁到 36pt 顶栏，而 `#modal` 是壳页内的 HTML 浮层 → 弹窗落在顶栏那一条里，看不见也点不到。现在确认弹窗与抽屉/命令面板同规：打开时让工作台让位（`hide_workbench_cmd`），关闭时按守卫恢复
- 已安装插件列表：名称与「来源（类型/位置）」两列之间的空白过大（名称列吞掉剩余宽度），改为两者相邻、卸载按钮仍靠右


## 0.5.0 — 交互与跨平台修复（2026-09-15）

### 新增

- **快捷键跨平台化并在工作台内可用**：`⌘K` / `⌘1`–`⌘4` 在 Windows 显示为 `Ctrl+K` / `Ctrl+1`–`4`；焦点在 dsh 工作台里时由注入脚本把按键转发回壳页处理
- **插件管理**：支持**本地插件目录绝对路径**安装；列表每行显示**来源**（npm / Git 源 / URL / 本地路径），行内省略号截断、悬停看全
- **托盘悬停提示**应用名（Windows 生效；macOS 状态项无 tooltip）
- **管理抽屉左边缘可拖动调宽**（双击复位、宽度持久化、窄窗自动转整幅）
- 折叠态把手条 18px → 8px，折叠后工作台向上扩展更多
- 引导页版本输入框加长度上限与「列表只显示最近 5 个」说明；从列表选择版本后清空手输值

### 修复

- **壳页事件监听被 Tauri ACL 静默拒绝**（`event.listen not allowed … core:event:allow-listen`）：`shell:shortcut` 转发因此失效——在工作台里按 `Ctrl/⌘+K`、`Ctrl/⌘+1`–`4` 不生效。新增 `capabilities/shell-events.json`，只给主窗授监听事件权限
- **卸载失败后「没窗口也退不出」**：卸载线程 panic 时标志未复位，退出被永久拦截；主窗销毁后缓存 handle 仍指向死窗口，重建分支进不去
- **卸载确认弹窗 fail-open**：弹窗内容尚未返回时按 `Esc` / `✕` 被当成「确认卸载并删数据」
- **Windows 关到托盘后再次启动 App 毫无反应**（单实例回调未清「关到后台」标记；macOS 由 Dock 点击兜底）
- **安装/切换的并发安全**：临时目录与 `current` 标记改为带进程号，避免 GUI 安装与 CLI 更新互撞；版本号在函数入口统一校验
- 崩溃自愈不回收已退出的 dsh 子进程（僵尸 / 句柄泄漏）；`stdout` 缺失时子进程成孤儿并占 profile 锁
- **卸载**：漏清工作台认证 cookie 存储与偏好；新增「删除目标必须是绝对路径」校验；Windows 卸载不再误杀用户终端里自己运行的 dsh
- **App 更新**：安装包命名截断导致残留；DMG 挂载失败路径不卸载挂载点（挂载点泄漏）；远端 tag 只接受 `[0-9A-Za-z.+-]`（防越界写入）；安装包校验 SHA-256 与大小
- **日志脱敏**：URL 里的 userinfo 凭据（`https://user:pass@…`）与敏感 query 一并脱敏
- 顶栏被页面横向滚动滚出视口（「管理行消失」）；浮层状态跨「关到后台」存活导致的召回残留
- 折叠/展开动画与 CSS 曲线同步（消除「抖两下」与不同步）；全屏下工作台几何错 32pt
- 启动页打开管理抽屉时，加载文案被抽屉边缘切掉
- 提示条压在顶栏上 / 关到后台后仍显示；折叠命令与顶部把手行为不一致
- 首次安装引导期间按快捷键打开的面板会被引导层盖住
- 先开命令面板（未开抽屉）时，氛围信息卡的「工作台 / dsh」显示为「—」（面板入口漏刷新）

### 内部

- `capabilities/shell-events.json`：壳页只授 `core:event:allow-listen`；`workbench-shortcut.json` 仍只授工作台 `core:event:allow-emit`
- 单元测试补充：日志脱敏、发布 tag 白名单、registry 版本存在性、卸载目标绝对路径、临时目录命名
- 一键安装脚本（macOS / Windows）增加 **SHA-256 校验**，并修正若干真实缺陷：PowerShell 7 下校验必失败（`.Content` 对非文本 MIME 为空）、Windows 闭包目录基准写成 `%LOCALAPPDATA%`（应为 `%APPDATA%`，导致升级前子进程清理静默失效）、macOS 在 Rosetta 终端里被误判为 Intel 而拒装、`pkill -f` 模式缺闭包目录（会误杀终端里手动跑的 dsh）、下载无重试且硬超时 600s；Windows 脚本修正 currentUser 安装路径探测（原先探测不到安装结果，装完不自动启动）
- `bump-version.sh` 增加**后置一致性断言**（5 处版本源必须都等于目标值，否则 exit 1——避免 CI 依 tag 重 bump 后版本错位）；CI 新增 `scripts-syntax` job（`bash -n` 全部 .sh + pwsh 解析两个 .ps1）
- `docs/regression-checklist.md` 与实现对齐（托盘项数、NSIS `/S /R`）

## 0.4.2 — 弹窗 label 泄漏修复（2026-09-14）

### 修复

- 自绘弹窗只关闭 webview 而未销毁窗口，窗口 label 被永久占用——**取消一次确认弹窗后，后续所有弹窗都不再出现**

## 0.4.1 — Windows 卸载修复（2026-09-14）

### 修复

- Windows 卸载确认页选「取消」时出现「数据已删、程序还在、界面已退出」的中间态：改为静默卸载（`/S`），不再二次确认
- 便携版（无 `uninstall.exe`）卸载时误报「应用数据已清理」却什么都没删，现在自行清理数据

## 0.4.0 — 无缝启动与稳定性（2026-09-14）

### 新增

- **无缝启动**：启动即显示加载页（品牌 + 「正在启动 dsh 工作台…」），dsh 首帧就绪后工作台移入——窗口出现的第一次呈现就是工作台
- 启动页信息行（App 版本 · dsh 版本 · 工作台端口）；命令面板与 ⌘K 系统菜单快捷键

### 修复

- **工作台空白根因**：累积的 dsh 会话 cookie 使 Cookie 头超过 16KB，dsh 返回 431——导航前清理陈旧 cookie
- 「加载页消失后先出现背景、才出现 dsh」的启动空档
- 刷新/导航白闪（关闭 WebView 自身背景绘制 + 启动即注入深色底）
- 壳页与工作台两层重叠导致的光标闪烁（工作台可见时把壳页裁到只剩顶栏）
- 标题栏补偿坐标错误引发的「管理行消失」与「内容显示不全」
- 代码审查修复（前端 + 后端）：管理命令失效、Windows 三项行为、就绪态与日志

## 0.3.0 — 瘦壳重设计（2026-08-23）

**产品定位重构**：从「内置 340MB dsh 闭包的胖壳」变为「只内置 node + npm + pnpm 的瘦壳」（安装包约 50–180MB），dsh 按需从 npm registry 安装。

### 新增

- **首次引导安装**：无 dsh 闭包时显示引导页——默认安装 latest，可展开高级选项（registry 源 + 指定版本），实时进度、可取消、失败重试
- **dsh 版本管理**（dsh 页）：当前版本、启动静默检查新版、一键更新到 latest、版本列表（registry 全部版本）任意安装/切换、自动回滚到上一版本；registry 源可持久化配置（npmjs / npmmirror）
- **App 自身更新**（关于页）：检查 GitHub Releases、一键下载安装（macOS DMG / Windows NSIS 静默）、「在浏览器打开下载页」手动兜底
- **插件管理**（插件页）：已安装插件列表（web profile，与终端共用）、安装/卸载、实时输出、完成后自动重启工作台
- **CLI 无头工具**保留：`dsh-desktop --self-update-check` / `--self-apply-update <ver>`

### 移除

- 局域网扫码远程连接（lan-proxy / mDNS / 二维码）
- 常规页（工作目录选择、登录自启、手动重启、日志查看 UI）
- 内置 dsh 闭包（resources/dsh）与上游自动发版（upstream-check workflow）

### 内部

- 模块化：`registry.rs`（npm 查询）/ `dsh.rs`（闭包管理：tmp→双重自检→原子切换→GC 保留上一版）/ `plugin.rs` / `appupdate.rs`
- 失败安全：更新/安装任何失败不动当前版本；`~/.dsh` 永不自动删除
- 壳页重构：4 Tab（工作台 / dsh / 插件 / 关于）+ 首次引导视图，设计稿由 open-design 生成（紫粉蓝光斑玻璃拟态 + 对比度工程）

### 修复

- 版本比较 rc 数字感知（`rc.10 > rc.9`）
- 安装切换失败窗口（`.old` 延迟删除）
- dsh 版本参数白名单校验（防 npm 参数注入）
- macOS x86_64 无 CI 产物时 App 更新走手动兜底
