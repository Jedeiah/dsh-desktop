# DSh Desktop 手工回归清单

发版前手工回归（覆盖瘦壳重设计 v0.3 以来的形态，行内标注了 0.5.0 新增项）。每项记录：✅ 通过 / ❌ 失败 / ⏭ 跳过（注明原因）+ 备注。

环境：macOS（arm64 本机）为主；Windows 项需 Windows 机器/虚拟机。App 构建后从 `.app` 包内运行（或用 `cargo tauri dev` 开发模式）。

## 1. 首次引导安装

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 1.1 | 全新安装（无闭包） | 清空 `<app-data>/com.dsh-desktop.app`（备份后删），启动 App | 出现引导页（不启动 dsh 进程）；默认「安装最新版（latest）」 | |
| 1.2 | 默认安装 latest | 引导页点「安装」 | 显示下载/安装进度（npm 实时输出）→ 双重自检 → 自动进入工作台（dsh web UI 正常） | |
| 1.3 | 指定版本安装 | 引导页展开高级选项 → 选择指定版本（如上一版 rc）→ 安装 | 安装该指定版本；dsh 页「当前版本」显示该版本 | |
| 1.4 | 断网/失败重试 | 断网后启动 → 引导页安装 | 显示错误 + 重试按钮，提示检查网络；恢复网络后点重试成功 | |
| 1.5 | 安装中取消 | 安装进行中点「取消安装」 | 安装终止、tmp 清理；引导页可再次安装；`<app-data>/dsh/` 无残留 `-tmp` 目录 | |
| 1.6 | 引导页 registry 换源 | 高级选项填 `https://registry.npmmirror.com` → 安装 | 安装走该源成功；dsh 页 Registry 源显示一致 | |
| 1.7 | 已安装再启动 | 1.2 完成后重启 App | 不出现引导页，直接加载工作台 | |
| 1.8 | 安装进度文案无噪声 | 引导页点「安装」（冷装更明显） | 阶段文案不出现 pnpm 的纯 `+` 条形进度（「一行加号」）；`Packages: +N` 等含内容行照常显示。后端手动 e2e：`cd apps/desktop/src-tauri && cargo test -- --ignored --nocapture setup_install_stream`（需网络） | |

## 2. dsh 版本管理（dsh 页）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 2.1 | 当前版本展示 | 打开 dsh 页 | 显示当前运行版本；版本列表含已安装版本 | |
| 2.2 | 更新到 latest | 上游有新版时 dsh 页点「更新到最新」 | 进度 → 自检 → 原子切换 → 工作台自动重启为新版；dsh 页当前版本更新 | |
| 2.3 | 指定版本安装 | 版本列表选一个非当前版本 → 确认 | 安装成功，工作台自动重启为该版本 | |
| 2.4 | 回滚到上一版本 | 在版本列表选上一版本安装 | 回滚成功；工作台运行上一版本 | |
| 2.5 | 版本保留策略 | 连续安装 3 个版本后检查 `<app-data>/dsh/` | 只保留当前 + 上一版本；更旧版本被 GC | |
| 2.6 | 安装失败不动当前 | 断网/错误版本号触发安装 | 失败提示，当前版本照常运行；无 `-tmp` 残留 | |
| 2.7 | 启动静默检查提示 | 有新版时启动 App → dsh 页 | dsh 页显示「有新版本」提示（不自动安装） | |
| 2.8 | 无新版 | 已是最新时打开 dsh 页 | 显示「已是最新版本」类状态 | |

## 3. 插件管理（插件页）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 3.1 | 空列表 | 未装插件时进入插件页 | 显示空列表（不报错） | |
| 3.2 | 列表展示 | 终端已装插件后进入插件页 | 显示已装插件名称/版本/状态（读取 `~/.dsh/profiles/web`），并显示**来源**（`npm · 版本` / `Git 源` / `URL` / `本地 · 绝对路径`）；长路径在行内省略、悬停可看全 | |
| 3.2b | 本地插件安装（0.5.0） | 输入本地插件目录的**绝对路径**（如 `D:\plugins\my-plugin`）→ 安装 | 安装成功；列表该行来源显示「本地 · 绝对路径」；相对路径应被拒绝并提示 | |
| 3.3 | 安装插件 | 输入包名（如 `@linxin666/dsh-web-ui-all`）→ 安装 | 实时输出 pnpm 进度 → 成功提示 → **工作台自动重启**，插件生效 | |
| 3.4 | 卸载插件 | 列表选中 → 卸载 | 实时输出 → 成功 → 工作台自动重启，插件移除 | |
| 3.5 | 数据共用 | 安装后检查 `~/.dsh/profiles/web` | 与终端 dsh 共用同一份配置（终端可见该插件） | |
| 3.6 | 构建脚本门禁 | 安装带构建脚本的插件 | 自动写 `pnpm-workspace.yaml` 门禁（allowBuilds/minimumReleaseAge）并成功 | |

## 4. App 更新（关于页）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 4.1 | 有新版 | 关于页点「检查更新」 | 显示新版本提示；「下载并安装」可用 | |
| 4.2 | 无新版 | 已是最新时检查 | 显示「已是最新版本」 | |
| 4.3 | 断网 | 断网后检查 | 静默失败/提示无法连接，不崩溃 | |
| 4.4 | 下载并安装（macOS arm64） | 有新版时点「下载并安装」 | 下载（校验大小）→ 挂载 DMG → 弹系统授权复制到 /Applications → 退出当前实例 → 打开新版本 | |
| 4.5 | macOS x86_64 手动兜底 | x86_64 机器上有新版时检查 | 「下载并安装」不可用/提示不支持，**「在浏览器打开下载页」可用**（CI 无 x86_64 产物） | |
| 4.6 | 下载失败 | 断网/中断时点下载 | 失败清理临时文件，当前版本不受影响，可重试 | |
| 4.7 | Windows 安装（更新助手，未发布） | Windows 上有新版时点下载并安装 | 先结束 dsh/pnpm 子进程（日志「结束 dsh/pnpm 子进程后交给更新助手」）→ 助手写握手（日志「更新助手已就绪」）→ 当前进程退出 → 助手**在本进程消失后**才静默运行 setup.exe（`/S /R`）→ 新版窗口自动出现；版本号确实更新 | |
| 4.7b | Windows 助手未就绪时不退化成静默失败 | 临时让 PowerShell 不可用（如策略禁用/powershell.exe 改名）后点更新 | 界面报「更新助手未就绪…已取消本次更新」，**App 不退出**、dsh 被重新拉起、可重试；日志有对应行 | |
| 4.7c | Windows 上装完没自动起来时兜底拉起 | 正常更新（可人工让 `/R` 失效：装完后立刻结束新进程） | 助手在安装器退出后 2s 检查，无实例则再拉起一次，最终一定有新版窗口 | |
| 4.7d | Windows 等待窗口内本应用仍未退出则不装 | 让发起更新的进程在助手等待的 30s 内仍存活（如人为挂住退出路径） | 助手到点后**放弃安装**（不运行 setup.exe、不追加 `state=launching`），App 保持可用；可重试 | |
| 4.8 | App 更新中禁止其它操作 | 有新版时点「下载并安装」→ 观察关于页与 dsh 分段 | 进度条随下载推进（显示已下载/总量）；「检查更新」「下载」「卸载」与 dsh 分段的安装/切换/回滚**均禁用**，「在浏览器打开下载页」仍可用；下载完成进入校验安装态（不确定文案）→ 自动重启 | |
| 4.8b | 更新中后端拒绝重启/装卸 | 更新进行中，用命令面板「重启 dsh」或触发插件装卸（前端按钮已禁用，可用调试手段直接 invoke） | 返回「应用正在更新，请稍后再试」，不拉起 node/dsh（避免重新占住 `resources\node\node.exe` 的映像锁） | |
| 4.9 | 更新未生效时可诊断 | 人为让安装失败（如 Windows 上拦下安装器/把安装包删掉）后，隔 1~2 分钟以上再启动 App | 启动日志出现「上次更新到 vX 未生效（当前仍为 vX）」并弹系统通知；标记文件 `<app-data>/update-attempt.txt` 被清除（不重复报）。若在等待窗口内启动（安装已开始 120s / 仅发起 60s），只记「安装可能仍在进行」且**保留**标记（避免装完前又双击旧版导致假警报） | |
| 4.9b | 从旧版本更新到本版仍是旧逻辑 | 在 0.5.2 上应用内更新到 0.5.3 | 走的仍是 0.5.2 内的旧逻辑（可能失败）；失败时下次启动按 4.9 给出提示。**新链路从 0.5.3 起才生效**（见 CHANGELOG） | |
| 4.10 | 更新成功后不再误报 | 正常走完一次 App 内更新 | 新版启动日志出现「已更新到 vX」；无「未生效」通知 | |

## 5. 卸载（关于页）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 5.0 | 确认弹窗 | 点任一卸载按钮 | 弹出**自绘确认弹窗**（居中于主窗）：按钮为「确认卸载 / 取消」；取消 → 无操作、按钮复位 | |
| 5.1 | 仅卸载应用 | 点「仅卸载应用」→ 弹窗确认 | 结束 dsh → 清应用数据/WebView 缓存 → App 移入废纸篓/回收站 → 退出；`~/.dsh` 保留 | |
| 5.2 | 卸载并删除 ~/.dsh | 点「卸载并删除 ~/.dsh」→ 弹窗确认（提示不可撤销） | 同上，且 `~/.dsh`（macOS）/ `%USERPROFILE%\.dsh`（Windows）被删除 | |
| 5.3 | Windows 唯一卸载链 | 安装后从「设置 → 应用」触发卸载 | 唤起系统卸载器，程序文件与数据彻底清理（`uninstall.exe` 内置同款清理） | |
| 5.4 | Windows 静默卸载 | 安装版点「仅卸载应用」→ 确认 | 不再出现系统卸载器确认页（`/S` 静默）；程序文件、注册表项、快捷方式被删除；应用数据/WebView 缓存被清理；`~/.dsh` 保留 | |
| 5.5 | Windows 便携版卸载 | 解压版运行中点「仅卸载应用」→ 确认 | 无 `uninstall.exe` → App 自行清理应用数据/WebView 缓存 → 退出；通知提示便携版需手动删除所在文件夹 | |
| 5.6 | 卸载无残留（macOS） | 卸载后检查 `~/Library/` 下 `Application Support`、`Caches`、`WebKit`、`HTTPStorages`、`Preferences`、`Saved Application State` 里本 App 条目（`Logs`、`Cookies` 本 App 不使用，作反向核对） | **全部不存在**；特别是 `<bundle-id>.binarycookies`（文件形态）**与 `<bundle-id>/`（目录形态，内含 httpstorages.sqlite；本机实测同一目录下 91 个目录形态 / 12 个文件形态）**，以及 `<bundle-id>.plist`；`~/.dsh` 按所选档位保留/删除 | |
| 5.6b | 卸载后 cookie 不再重现 | macOS 卸载完成后**等 5 秒以上**再检查 `~/Library/HTTPStorages/<id>.binarycookies`（以及 `<id>/` 目录形态） | 文件不存在（由脱离本进程的 shell 在退出后补删；实测修前会在 App 退出同秒重现） | |
| 5.7 | 卸载无残留（Windows） | 卸载后检查 `%APPDATA%`、`%LOCALAPPDATA%` 下本 App 条目 | 全部不存在；`~/.dsh`（`%USERPROFILE%\.dsh`）按所选档位保留/删除 | |
| 5.5b | 安装版但卸载器起不来 | 让 `$INSTDIR\uninstall.exe` 无法启动（如重命名/安全软件拦下）后点「仅卸载应用」 | **不冒充便携版去删数据**；弹「卸载未完成：无法启动系统卸载器…」，主窗与工作台都回来（可直接重试）；程序文件与 app 数据保持原样——注意「完全卸载」档位会提示 `~/.dsh` 已按选择删除 | |
| 5.5d | 卸载残留有提示 | 先人为制造占用（如手工起一个 `node.exe` 跑 `resources\node\node.exe`，或用工具占住 `$INSTDIR` 里某个文件），再从系统「设置 → 应用」卸载 | 卸载结束弹一次提示框，列出 `$INSTDIR` / `%APPDATA%\<id>` / `%LOCALAPPDATA%\<id>` 三处需手动删除的残留；无占用时**不弹**（不打扰正常卸载） | |
| 5.5c | 卸载清掉临时区的更新包 | 先让临时区存在 `dsh-desktop-update-*`（如更新失败留下），再卸载 | 卸载时被清掉（日志「清理遗留安装包」）；临时区其它文件不受影响 | |
| 5.8 | 更新包不留残留 | 走一次 App 内更新，更新后看临时目录 | 无 `dsh-desktop-update-*` 文件（Windows 上安装包要留给助手在退出后使用，最迟在下次启动且超过 1 小时 TTL 时被清扫，日志出现「清理遗留安装包」） | |

## 5b. 快捷键与托盘

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 5b.1 | 键帽文案随平台 | Windows 看顶栏「管理」/启动页/氛围层 | 显示 `Ctrl+K`、`Ctrl+1–4`（macOS 为 `⌘K`/`⌘1–4`） | |
| 5b.2 | 焦点在壳页 | 点顶栏「管理」后按快捷键 | `Ctrl/⌘+K` 开关命令面板，`Ctrl/⌘+1–4` 切区域 | |
| 5b.3 | 焦点在工作台（Windows） | 点 dsh 页面内部（焦点进工作台）后按 `Ctrl+K`、`Ctrl+1–4` | 仍能打开命令面板 / 切区域（注入脚本经 `shell:shortcut` 转发；**依赖主窗的 `core:event:allow-listen` 授权**，见 capabilities/shell-events.json——缺它则静默失效，只有轮询兜底看不出来） | |
| 5b.4 | 焦点在工作台（macOS） | 同上，按 `⌘K` / `⌘1–4` | `⌘K` 另有系统菜单加速键兜底；`⌘1–4` 依赖 `shell:shortcut` 转发。Esc 不转发（dsh 自己会用到） | |
| 5b.5 | 托盘悬停提示 | 鼠标悬停托盘图标（Windows） | 显示 app 名称 `DeepSeek Harness Desktop` | |
| 5b.6 | 抽屉聚焦输入框 → 关到后台 → 召回 | 开抽屉 → 点进任一输入框 → 红点/⌘W 关到后台 → 托盘召回 | 顶栏（管理行）仍在视口内、无横向滚动（曾出现顶栏被滚出视口且不自愈）；抽屉已收起；工作台正常 | |
| 5b.7 | 折叠/展开有过渡 | 点右上角折叠按钮 → 再点顶部把手 | 顶栏平滑滑出/滑入，工作台同步平移（不是「一跳」）；把手淡入淡出 | |
| 5b.8 | 抽屉宽度可拖（0.5.0） | 开抽屉 → 拖左边缘 ←/→ → 双击左边缘 | 拖动跟手、有最小/最大值夹紧；双击回到默认宽度；重启 App 后宽度保持；窄窗下抽屉自动整幅 | |
| 5b.9 | 打开浮层时的工作台让位（0.5.0） | 工作台可见时按 `⌘/Ctrl+2`（或点「管理」） | 浮层**按整幅尺寸首帧出现**（不再先按顶栏那条 36pt 高度布局再跳变）；抽屉/面板收起后工作台自动归位 | |
| 5b.10 | 确认弹窗不被原生工作台盖住 | 工作台可见时 ⌘K → 输入 `重启` → 选「重启 dsh」 | 确认弹窗**整幅可见可点**（壳页未被裁到 36pt）；取消后工作台归位 | |
| 5b.11 | 命令面板「重启 dsh」 | 同上并点「重启」 | dsh 换端口重启（日志出现新的 `dsh web:`）；这是**显式重启**——工作台会显示并重连，随后带新运行时归位；安装/更新进行中应被拒绝 | |
| 5b.12 | 抽屉里发起插件装卸 / 版本更新时不踢人 | 打开抽屉 → 装/卸插件（或切版本）→ 确认 | 抽屉**不被关掉、壳页不重载**（输出面板与分段状态保留）；新运行时就绪后收起抽屉，工作台带新代码归位 | |

## 6. 稳定性

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 6.1 | 崩溃自愈 | 手动 kill dsh 子进程 | 自动重启（指数退避 2s→4s→8s→15s）；连续 5 次后停止并弹窗提示日志路径 | |
| 6.1b | 致命错误弹窗给出路 | 连续 kill dsh 5 次触发「运行异常」弹窗 | 弹窗有「退出」「重试」两键：点**退出**→ 应用真正退出（不留后台残留）；点**重试**→ 崩溃计数清零并重新拉起 dsh（日志 `用户选择重试`）；✕/Esc 等同重试（不会误退应用） | |
| 6.2 | 单实例 | 已运行再次启动（双击/安装脚本） | 不产生第二个实例/双托盘 | |
| 6.3 | 托盘 | 关闭窗口 → 托盘操作 | 窗口隐藏进托盘；托盘两项：显示主窗口 / 退出 均生效（「管理台」已并入壳页抽屉）；退出连带结束 dsh 无孤儿 | |
| 6.4 | 退出无孤儿 | 托盘退出后检查进程 | `dsh-desktop` 与 dsh 子进程均退出；终端手动跑的 dsh 不受影响 | |

## 7. Windows 专项（需 Windows 机器）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 7.1 | 安装取消 taskkill | 引导安装中取消 | `taskkill /PID <npm> /T /F` 生效，tmp 清理 | |
| 7.2 | NSIS 静默安装 | 运行 setup.exe 带 `/S /R` | 静默装到当前用户 `%LOCALAPPDATA%\DeepSeek Harness Desktop`（currentUser 模式），由 `/R` 自动启动 | |
| 7.3 | 安装脚本退出已运行实例 | 已运行时跑 install.ps1 | 退出实例 + 精确结束本 App 的 dsh 子进程（命令行特征 `bin.js … --profile web`，不误杀用户其它 node） | |
| 7.4 | 便携版 | 解压 `DeepSeek-Harness-Windows-x64.zip` 直接运行 | 正常引导安装 dsh 并进入工作台 | |
| 7.5 | 中文路径/空格路径 | 用户名为中文/带空格时全流程 | 安装、引导、更新、卸载均正常 | |

## 8. CLI 钩子（无 GUI 自检）

| # | 场景 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|
| 8.1 | 更新检查 | `.app/Contents/MacOS/dsh-desktop --self-update-check` | `UP_TO_DATE` / `UPDATE_AVAILABLE current=… latest=…` | |
| 8.2 | 应用更新 | `--self-apply-update <ver>` | `APPLIED <ver>`；失败时 `APPLY_ERROR` 且当前版本不受影响 | |
| 8.3 | 卸载测试 | `--self-uninstall-test` | `UNINSTALL_DONE`（只删数据，不删自身） | |
| 8.4 | 完全卸载 | `--self-uninstall-full [--wipe]` | 结束实例+子进程、清数据（NSIS PREUNINSTALL 钩子调用） | |

---

## 备注

- 每次回归前确认上游 dsh 版本与 registry 可达；断网项可在系统层面临时断网/代理模拟。
- 失败项记录到对应行备注（错误信息、复现步骤），发版前必须清零。
